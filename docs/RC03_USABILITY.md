# RC03 사용성 개선

작성일: 2026-07-25 (Asia/Seoul)

RC02 운영 인수시험 뒤 관리자 단독 사용에서 확인한 불편을 개선한다.
이 문서는 구현 결과와 자동 검증, 아직 수행하지 않은 A 기기 실기를 구분한다.

## 확정 동작

### 학생과 반

- 학생은 전체 이름, 매쓰홀릭 아이디와 비밀번호로 먼저 한 번 등록한다.
- 반별 학생 구성 화면에서 학생 마스터 목록의 여러 학생을 선택한다.
- 한 학생은 여러 반에 동시에 소속될 수 있고 모든 반에서 같은 QR을 사용한다.
- 반 삭제는 반과 그 반의 소속 관계만 삭제하며 학생, 자격정보와 QR은 보존한다.
- 로그인 정보 변경은 별도 창에서 새 아이디, 새 비밀번호와 비밀번호 확인값을 받는다.
- 수업 전에도 이번 수업에만 추가할 보강 학생을 선택할 수 있다.

### QR 카드와 전달

- 출력 문서는 A4 한 페이지 중앙에 실제 크기 65×90mm 세로 카드를 배치한다.
- 카드 안 QR은 30×30mm이고 상단에는 학생 전체 이름을 표시한다.
- 마스킹 표시명 입력·표시 동작은 제거했다. 기존 DB의 열은 스키마 v1 무중단
  호환을 위해서만 남기고 전체 이름을 동일하게 저장한다.
- Android 인쇄 서비스 직접 전송과 별도로 `카드 크기 PDF 공유·저장`을 제공한다.
- PDF는 Quick Share 등 Android 공유 대상에 전달할 수 있고 앱 cache의 임시본은
  복귀 뒤 삭제한다.

### QR 대기 화면

- QR 인식은 전면·후면 카메라를 선택할 수 있지만 카메라 미리보기는 표시하거나
  저장하지 않는다.
- 화면에는 QR을 맞출 위치와 현재 상태만 표시한다.

### Web 학생 화면

- 학생 확인이 끝나면 학습지 페이지를 자동으로 연다.
- 목록 화면 위에는 `학습지`와 `진단평가` 두 전환 버튼만 제공한다.
- 학생 모드의 상위 주소는 학습지, 진단평가와 문제 화면으로 제한한다.
- Android 뒤로가기는 항상 소비하고 시스템 탐색 막대는 숨김 상태를 재적용한다.
- 로그인·로그아웃·복구 중 WebView를 숨기고 완전 불투명 차단 화면만 표시한다.
- 기존 중앙 상단 상태 배지는 학생 화면에 표시하지 않는다.
- 문제 화면의 입력칸, 수식 입력 버튼, 모름·다음 문제·답안 제출 버튼의 최소
  터치 영역과 글자 크기를 늘리고 문제 영역에 위·아래 여백을 보강한다.
- 전체답안 화면의 최종 제출 버튼은 화면 하단에 고정한다.
- 종합분석 화면은 의미 구조가 확인된 경우 즉시 불투명 보호 화면으로 가리고,
  틀린 문제 번호 목록만 표시한다. `확인하고 채점 끝내기`를 누르면 자동
  로그아웃하고 Kiosk의 QR 대기로 복귀한다.
- 허용된 학생 페이지가 새 문서로 이동할 때도 WebView를 먼저 숨기고 검증이
  끝난 뒤에만 다시 표시해 종합분석 상세가 DOM 차폐보다 먼저 보이는 시간을
  막는다.

### 수업 시작·종료

- 수업 시작은 먼저 남은 Web 세션을 안전 정리하고 성공한 경우에만 선택한 반
  수업을 시작한다.
- 수업 종료는 먼저 Web 로그아웃과 정리를 끝내고 성공한 경우에만 현재 수업과
  일회성 보강 명단을 종료한다.
- 정상 관리자 흐름에서 별도의 `Web 세션 안전 정리` 단계를 요구하지 않는다.

## 검증 상태

- Kotlin 소스와 Kiosk/Web 계측시험 소스 컴파일: 통과
- 네 모듈 전체 debug 회귀: 204 tasks, 통과
- JVM 단위시험: Probe 8, 잠긴 POC 1, Web POC 24, Kiosk 17,
  총 50개 실패 0
- 네 모듈 debug lint: 오류 0
- 네 debug APK 빌드: 통과
- 서명된 release 빌드: 158 tasks, 통과
- release APK 버전·권한·`debuggable=false`·v2 서명·단일 signer·두 앱
  signer 일치·Debug signer 거부·zipalign: 통과
- release signer SHA-256:
  `9d5bd7d9c328df2e5c54b67d1aa2d42caef2674eeace0614bfe2d37c7651f5b7`
- A 설치 RC03 Kiosk `0.6.0-rc03`:
  `5546B399361227A38039AC0464A18DAE51DC17093B6DB530BEEFD0ABBD022AE6`
- A 설치 RC03 Web POC `0.4.0-rc03`:
  `64A8C7D6DFE97B8BBBD0591CBABF1E538E082EC86AC4721D8350C97B64944A3E`
- Android 13 일회용 에뮬레이터 계측시험:
  - A 설치 시점 기준 Web 30개, Kiosk 10개, 실패 0
  - RC04 보강 소스 기준 Web 33개, Kiosk 11개, 실패 0
- A `install -r`: 두 앱 성공. UID·최초 설치 시각·데이터 경로, release
  signer, Device Owner와 전용 HOME 유지
- A HOME·최근 앱·뒤로가기 차단: 통과, Lock Task `LOCKED` 유지
- A 실제 사이트·카메라·PDF·프린터 RC03 회귀: 미수행
- A 기기 직접 프린터 전송 실패 읽기 전용 진단: 수행. 네트워크 도달은
  확인했지만 기존 작업이 취소 중 정체된 정확한 원인은 아직 미확정

결과 페이지 선차폐, 비대칭 프린터 DPI와 SPA 인코딩 경로 차단 보강은
자동시험을 통과해 상위 versionCode의 RC04에 포함했고 A에도 보존형
업데이트했다. 실제 사이트와 물리 출력 회귀는 여전히 미수행이다.

## A 기기에서 확인할 항목

1. 보존형 업데이트 뒤 기존 학생·반 목록이 관리자 화면에서 정상 로드되는지
2. 학생 한 명을 두 반에 넣고 동일 QR로 각 반 수업에 진입하는지
3. 한 반 삭제 뒤 학생과 QR이 다른 반에서 유지되는지
4. 65×90mm 카드와 30×30mm QR이 실제 크기로 출력되는지
5. PDF를 Quick Share로 PC에 직접 보낼 수 있는지
6. 전면·후면 카메라 모두 미리보기 없이 QR을 인식하는지
7. 자동 학습지 진입과 학습지↔진단평가 전환이 되는지
8. 문제 입력·모름·다음·두 답안 제출 버튼이 충분히 커졌는지
9. 종합분석 상세 내용 없이 오답 번호만 보이고 확인 뒤 QR 대기로 복귀하는지
10. Web 학생 화면에서 뒤로가기·홈·최근 앱·알림창으로 이탈하지 않는지
11. QR 대기와 Web 문제 화면에서 태블릿을 180° 뒤집을 때 세로로 전환되지
    않고 반대 가로 방향으로 회전하는지

## 2026-07-27 최신 실물 확인과 RC30/RC32 재검증 대기

- 통과: QR·Web 두 가로 방향 회전, 최상단·계정 메뉴 비표시, 오류신고·해설
  문구 비표시, 제출 줄 간격, 수식 자동 전환과 모드 변경 숨김, 확인창의 사이트
  주소 머리말 제거
- 실패 재현: 학습지→진단평가 전환 지연, 문제 동영상 잔류, 분수 선택 오삭제,
  입력 전 필기 버튼 잔류, 전체답안 자동 이동·풀이 업로드 제거 실패,
  25문항 `RESULT_INCOMPLETE`
- QR 중앙 사각형은 카메라 영상이 아니라 숨겨진 분석 좌표의 목표 범위인데
  설명이 부족했고 좌우 안내가 상하보다 민감했다.
- RC30/RC32에서 분수 선택을 복원하고 `기본`만 숨기도록 바로잡았다. 동영상
  이미지 패널, 입력칸 suffix의 필기 제어, 실제 Ant 전체답안 모달과 업로드
  구조를 대상으로 수정했다.
- 25문항 결과는 내부 스크롤을 점진 이동해 지연 카드를 만든 뒤 화면의
  `문항수`와 1..N 분류가 일치할 때만 확정한다. 자동 계측에서 25개 전체
  수집을 확인했고 RC32 실제 사이트의 25문항 오답 목록도 정상 통과했다.
- RC30/RC32 보존형 업데이트 뒤 관리자 복구, 기존 `토요일2` 반과 학생 3명
  보존, 같은 반 수업 재시작과 `QR_READY` 복귀를 실물 확인했다.
- 중앙 목표 사각형은 물리 카메라 렌즈 위치를 나타내지 못하는 잘못된
  표현이므로 `fed3fe5`에서 제거했다. 현재 A 설치본에는 아직 포함되지
  않았으며 다음 묶음 업데이트에서 반영한다.
